/*
원시 타입 하나의 값만 지정 가능한 타입
*/
{
	//기본 원시타입 5개
	//number => 123, 0.123, Infinity(양의 무한대). -Infinity(음의 무한대), NaN
	let num1: number = 123

	//string => ``, '', "", `hi ${arr}`
	let str1: string = 'hi'

	//boolean
	let bool1: boolean = true

	//null
	let null1: null = null

	//undefined
	let unde1: undefined = undefined
}
