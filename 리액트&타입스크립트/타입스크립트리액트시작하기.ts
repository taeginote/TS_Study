{
	/**
	 * 터미널
	 *
	 * 1. React App 설치 (아래 점은 더 폴더에서 새로운 폴더를 생성하지말고 저기서 만들어라 라는 의미이다.)
	 * npx create-react-app .
	 * 이러면 react app이 깔림
	 * 2. 리액트에 타스 적용하는법 (타입 선언 패키지 설치)
	 * npm i @types/node @types/react @types/react-dom @types/jest
	 * 위에 4가지 설치
	 * 3. tsconfig.json 만들고
	 *
	 */
}
